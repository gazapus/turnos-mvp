/**
 * Stub de la agenda de recepción.
 *
 * @returns Página placeholder post-login (rol RECEPCIONISTA).
 */
export default function AgendaPage() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center gap-4 bg-background p-8">
      <h1 className="font-heading text-3xl font-bold text-foreground">
        Agenda
      </h1>
      <p className="text-muted-foreground">Gestión de turnos — próximamente.</p>
    </main>
  );
}
